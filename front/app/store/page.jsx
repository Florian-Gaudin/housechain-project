import Header from "@/components/include/_header";

const Store = () => {
    return (
        <div className="bg-bg relative isolate pt-14 bg-fixed bg-center bg-cover bg-no-repeat bg-home min-h-[100vh]">
            {/* barre de recherche; filtres,   */}
        </div>
    );
};

export default Store;
